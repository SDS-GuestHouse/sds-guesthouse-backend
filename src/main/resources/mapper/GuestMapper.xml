<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.sds_guesthouse.model.dao.GuestDao">

    <select id="countByUserId" resultType="int">
        SELECT COUNT(*)
        FROM guest
        WHERE user_id = #{userId}
    </select>

    <insert id="insertGuest">
        INSERT INTO guest (user_id, password, name, phone)
        VALUES (#{userId}, #{password}, #{name}, #{phone})
    </insert>

    <select id="countByUserIdAndPassword" resultType="int">
        SELECT COUNT(*)
        FROM guest
        WHERE user_id = #{userId}
          AND password = #{password}
    </select>

</mapper>